# Backups

Real machine snapshots are never committed here. `install.sh` stores them in:

```text
~/desktop-backups/void-experience/<timestamp>/
```

This directory documents the policy only. Backups may contain application
launchers and user paths, so `.gitignore` excludes all other content.
